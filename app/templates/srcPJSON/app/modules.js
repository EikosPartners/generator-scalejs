/* global require */
define([
    // This hook is needed for the generator to add modules
    /*========Yeoman Hook=======*/
    'scalejs!application/app/template/templateModule'], 
function (
    app
) {
    'use strict';

    return arguments;
    
});