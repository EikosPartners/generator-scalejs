lint:
    - 'newer:coffeelint'

compile:
    - 'lint'
    - 'newer:coffee'

config:
    - 'bower'

build:
    - 'compile'
    - 'config'
    - 'requirejs'
    - 'uglify'
    - 'copy'

serve:
    - 'connect'
