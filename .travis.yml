language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-scalejs' ]; then cd .. && eval "mv $currentfolder generator-scalejs" && cd generator-scalejs; fi

